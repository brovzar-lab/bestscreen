.DS_Store
node_modules/
*.log
v3-selections.json
.firebase/
firebase-debug.log
.cache/
.playwright-mcp/
.superpowers/

# Loose verification / Playwright screenshots — kept locally for reference,
# never committed. If you want to ship a screenshot, put it in docs/.
*.png
!docs/**/*.png

# Local config — secrets live here, never commit
.env
config.local.js
